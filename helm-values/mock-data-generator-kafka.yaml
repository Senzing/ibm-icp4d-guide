image:
  pullPolicy: Always

senzing:
  subcommand: url-to-kafka
  kafkaBootstrapServerHost: kafka-external
  kafkaBootstrapServerPort: 9092
  kafkaTopic: senzing-kafka-topic
  inputUrl: "https://s3.amazonaws.com/public-read-access/TestDataSets/loadtest-dataset-1M_shuffled.json"
  recordMax: 10000
  recordMonitor: 1000
