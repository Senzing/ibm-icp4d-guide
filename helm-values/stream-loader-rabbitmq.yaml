image:
  pullPolicy: Always

replicaCount: 10

senzing:
  subcommand: rabbitmq
  databaseUrl: "${SENZING_DATABASE_URL}"
  dataSource: TEST
  entityType: TEST
  logLevel: info
  monitoringPeriod: 60
  persistentVolumeClaim: senzing-pvc
  rabbitmqHost: senzing-rabbitmq
  rabbitmqPassword: passw0rd
  rabbitmqQueue: senzing-rabbitmq-queue
  rabbitmqUsername: user
  threadsPerProcess: 4
