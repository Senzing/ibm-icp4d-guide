image:
  pullPolicy: Always

imagePullSecrets:
  - name: ${DOCKER_REGISTRY_SECRET}

securityContext:
  enabled: true
  runAsUser: 0
  runAsGroup: 0
  fsGroup: 0

senzing:
  optSenzingClaim: senzing-pvc
  subcommand: install
